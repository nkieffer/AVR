#include <avr/io.h>

ISR(INT0_vect){
  PORTB ^= _BV(PB6);
}
void setup(void){
  DDRB &= ~_BV(PB4);
  PORTB |= _BV(PB4);
  GIMSK |= _BV(INT0);
  MCUCR |= _BV(ISC0) | _BV(ISC1);
  sei();
}
void loop(void){

}
int main(void){
  setup();
  while(1) loop();
}
